入门

一:变量

val 相当于java final
var 变量

二：函数定义
def max(x: Int, y: Int): Int = {   //函数定义
    if (x > y) x
    else y
  }
max(5,6)


基本类型
    Byte
    Short
    Int
    Long
    Char
    String
    Float
    Double
    Boolean

函数是对象：
    Rational 对象
    val oneHalf = new Rational(1,2)
    val twoThirds = new Rational(2,3)





